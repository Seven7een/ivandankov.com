---
import { SITE, NAV_LINKS } from '@/consts';

const currentPath = Astro.url.pathname;
---

<header class="border-b border-[var(--color-border)] px-6 py-4">
  <div class="max-w-4xl mx-auto flex flex-col sm:flex-row items-start sm:items-center justify-between gap-2 sm:gap-0">
    <a href="/" class="font-semibold text-lg text-[var(--color-text-primary)] hover:text-[var(--color-accent)] transition-colors">
      {SITE.title}
    </a>
    <nav aria-label="Main navigation" class="flex flex-wrap gap-x-5 gap-y-1 text-sm">
      {NAV_LINKS.map(({ href, label }) => (
        <a
          href={href}
          class:list={[
            'transition-colors',
            currentPath === href || (href !== '/' && currentPath.startsWith(href))
              ? 'text-[var(--color-accent)] font-medium'
              : 'text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)]',
          ]}
        >
          {label}
        </a>
      ))}
    </nav>
  </div>
</header>
